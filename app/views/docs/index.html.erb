 <div class="row">
<div class="col-md-6">
<div class="mb-3">
 <% @docs.each do |doc|%>
<h2><%= link_to doc.title, doc %></h2>
<p><%= time_ago_in_words(doc.created_at) %></p>
<p><%= truncate(doc.content, length: 50) %>
<% end %>
</div>
</div>
</div>
<%= link_to "Create Doc", new_doc_path, class: "btn btn-warning btn-lg mt-2" %>